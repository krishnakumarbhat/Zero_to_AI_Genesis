name: Deploy GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install frontend dependencies
        working-directory: Zero_to_AI_Genesis2/frontend
        run: |
          node -v
          npm -v
          npm ci

      - name: Verify next is installed
        working-directory: Zero_to_AI_Genesis2/frontend
        run: npx next --version

      - name: Clean stale build artifacts
        working-directory: Zero_to_AI_Genesis2/frontend
        run: rm -rf .next out

      - name: Build static export
        working-directory: Zero_to_AI_Genesis2/frontend
        env:
          GITHUB_PAGES: 'true'
          GITHUB_REPOSITORY: ${{ github.repository }}
          NEXT_TELEMETRY_DISABLED: '1'
        run: |
          echo "GITHUB_PAGES=$GITHUB_PAGES"
          echo "GITHUB_REPOSITORY=$GITHUB_REPOSITORY"
          npm run build:pages 2>&1 | tee /tmp/build.log || true
          BUILD_EXIT=${PIPESTATUS[0]:-1}
          if [ "$BUILD_EXIT" != "0" ]; then
            echo "::error::Build failed. Last 100 lines:"
            tail -100 /tmp/build.log
            exit 1
          fi

      - name: Upload build log on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: build-log-deploy
          path: /tmp/build.log

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: Zero_to_AI_Genesis2/frontend/out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4